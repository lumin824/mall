version: '2'
services:
  nginx:
    image: 'nginx'
    restart: always
    volumes: 
      - ./www://etc/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - '8801:80'
  php:
    image: 'php:7.4-fpm'
    restart: always